# 接口文档 v1

## 1. 文档说明

- 文档依据当前后端代码实现整理，覆盖 `auth-service`、`user-service`、`item-service`、`file-service`、`trade-service`、`chat-service`、`review-service`、`notice-service`。
- 文档范围包含：
  - 对外 REST 接口
  - 内部 REST 接口
  - `chat-service` 的 WebSocket 协议
- 文档中的路径统一省略尾部 `/`，但当前实现普遍兼容带尾部 `/` 的访问方式。
- 如文档说明与代码实现存在冲突，以当前代码行为为准。

## 2. 通用约定

### 2.1 服务地址

| 服务 | 默认端口 | Base URL |
| --- | --- | --- |
| auth-service | `8081` | `http://127.0.0.1:8081` |
| user-service | `8082` | `http://127.0.0.1:8082` |
| item-service | `8083` | `http://127.0.0.1:8083` |
| trade-service | `8084` | `http://127.0.0.1:8084` |
| chat-service | `8085` | `http://127.0.0.1:8085` |
| review-service | `8086` | `http://127.0.0.1:8086` |
| file-service | `8087` | `http://127.0.0.1:8087` |
| notice-service | `8088` | `http://127.0.0.1:8088` |

### 2.2 统一响应结构

除文件预览接口外，HTTP 接口统一返回：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | integer | `0` 表示成功，非 `0` 表示业务或认证错误 |
| `message` | string | 返回消息 |
| `data` | object / array / null | 业务数据，空响应时为 `null` |

### 2.3 鉴权约定

- Access Token 通过请求头传递：`Authorization: Bearer <accessToken>`
- 未携带或格式错误时，常见返回：

```json
{
  "code": 401,
  "message": "未登录或Token缺失",
  "data": null
}
```

- `Authorization` 头格式错误时，常见消息为：`Authorization头格式错误`
- Access Token 失效时，常见消息为：
  - `access token已过期`
  - `access token无效`
  - `token类型无效`
  - `token用户信息无效`

### 2.4 Refresh Token 约定

- `auth-service` 登录和刷新接口会通过 Cookie 下发 `refreshToken`
- 默认 Cookie 配置：

| 项 | 值 |
| --- | --- |
| Cookie 名 | `refreshToken` |
| Path | `/auth` |
| HttpOnly | `true` |
| SameSite | `Lax` |
| Secure | `false`（本地默认） |
| 过期时间 | `1209600` 秒（14 天） |

- `POST /auth/refresh`、`POST /auth/logout` 依赖该 Cookie，不使用请求体。

### 2.5 时间与格式

- 文档中的时间字段为服务端序列化结果，常见为 ISO 风格字符串。
- `notice-service` 的 `createdAt` 明确按 `Asia/Shanghai` 时区输出。
- 文件上传使用 `multipart/form-data`。

### 2.6 通用错误码

| code | 说明 |
| --- | --- |
| `0` | 成功 |
| `400` | 参数错误、请求体错误、校验失败 |
| `401` | 未登录、Token 无效、Refresh Token 失效 |
| `403` | 权限不足、用户被封禁、生产环境禁用模拟能力 |
| `404` | 资源不存在、接口不存在 |
| `409` | 状态冲突、重复提交、当前状态不允许操作 |
| `500` | 服务器内部错误 |
| `502` | 第三方地理服务调用失败 |
| `4001` | 认证失败（账号或密码错误） |
| `4002` | 用户已存在 |
| `4003` | 用户不存在 |

### 2.7 常用枚举

| 枚举 | 取值 |
| --- | --- |
| 商品交易方式 `tradeMode` | `ONLINE`、`OFFLINE`、`BOTH` |
| 订单交付方式 `deliveryMode` | `DIRECT`、`PICKUP_POINT` |
| 通知状态筛选 | `UNREAD`、`READ` |
| 举报类型 `reportType` | `ITEM`、`USER` |
| 举报状态 `status` | `PENDING`、`APPROVED`、`REJECTED` |
| 文件业务类型 `bizType` | `ITEM_IMAGE`、`AVATAR`、`CHAT_IMAGE` |
| 文件状态 | `UPLOADED`、`BOUND`、`DELETED` |
| 自提点状态 | `ENABLED`、`DISABLED` |
| 聊天消息类型 | REST: 当前代码支持任意非空字符串，约定值 `TEXT`、`IMAGE`；WebSocket: `AUTH`、`TEXT`、`READ` |

---

## 3. 认证模块（auth-service）

Base URL: `http://127.0.0.1:8081`

### 3.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 健康检查 | `GET` | `/auth/ping` | 否 | 服务存活检查 |
| 登录 | `POST` | `/auth/login` | 否 | 用户登录并下发 access/refresh token |
| 刷新令牌 | `POST` | `/auth/refresh` | 否 | 使用 refreshToken Cookie 刷新 access token |
| 登出 | `POST` | `/auth/logout` | 否 | 注销 refresh session 并清理 Cookie |
| 解析 Token | `GET` | `/auth/token/parse` | 是 | 校验并解析 access token |
| 注册 | `POST` | `/auth/register` | 否 | 普通用户注册 |
| 校园认证 | `POST` | `/auth/campus/verify` | 是 | 补充实名和学号并标记已认证 |

### 3.2 `GET /auth/ping`

- 说明：健康检查
- 请求头：无
- 请求参数：无
- 响应 `data`：`string`

```json
{
  "code": 0,
  "message": "ok",
  "data": "auth-service is running"
}
```

### 3.3 `POST /auth/login`

- 说明：支持用户名或手机号登录
- 请求头：`Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `account` | string | 是 | 登录账号，支持用户名或手机号 |
| `username` | string | 否 | `account` 的别名，代码通过 `@JsonAlias` 兼容 |
| `password` | string | 是 | 登录密码 |

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `accessToken` | string | Access Token |
| `expiresIn` | integer | access token 过期秒数，默认 `900` |
| `tokenType` | string | 固定为 `Bearer` |

#### 请求示例

```json
{
  "account": "18812345678",
  "password": "123456"
}
```

#### 响应示例

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiJ9.xxx",
    "expiresIn": 900,
    "tokenType": "Bearer"
  }
}
```

#### 额外说明

- 成功时响应头会下发 `Set-Cookie: refreshToken=...`
- `account` 为空返回 `400`
- 账号或密码错误返回 `401`，业务码通常为 `4001`

### 3.4 `POST /auth/refresh`

- 说明：通过 `refreshToken` Cookie 刷新 access token
- 请求头：无特殊要求
- 请求体：无
- Cookie：`refreshToken`

#### 响应 `data` 字段

与登录接口一致：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `accessToken` | string | 新 access token |
| `expiresIn` | integer | 有效期秒数 |
| `tokenType` | string | 固定 `Bearer` |

#### 成功响应示例

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiJ9.new",
    "expiresIn": 900,
    "tokenType": "Bearer"
  }
}
```

#### 常见错误

- `401 refresh token无效或已过期`
- 刷新失败时服务会主动清理 Cookie

### 3.5 `POST /auth/logout`

- 说明：注销 refresh session
- 请求体：无
- Cookie：`refreshToken`

#### 成功响应

```json
{
  "code": 0,
  "message": "ok",
  "data": null
}
```

#### 说明

- 即使 Cookie 不存在，也会正常返回成功
- 响应头会清理 `refreshToken` Cookie

### 3.6 `GET /auth/token/parse`

- 说明：校验并解析 access token
- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `userId` | string | 用户 ID |
| `username` | string | 用户名 |

#### 响应示例

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "userId": "u_1001",
    "username": "zhangsan"
  }
}
```

### 3.7 `POST /auth/register`

- 说明：注册普通用户
- 请求头：`Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string | 是 | 用户名，需唯一 |
| `password` | string | 是 | 密码 |
| `phone` | string | 是 | 手机号，需符合中国大陆手机号正则 `^1[3-9]\\d{9}$` |
| `nickname` | string | 是 | 昵称 |

#### 请求示例

```json
{
  "username": "zhangsan",
  "password": "123456",
  "phone": "18812345678",
  "nickname": "张三"
}
```

#### 成功响应

```json
{
  "code": 0,
  "message": "ok",
  "data": null
}
```

#### 业务规则

- 注册成功后默认：
  - `role = USER`
  - `status = ACTIVE`
  - `campusVerified = false`

#### 常见错误

- `4002 用户名已存在`
- `4002 手机号已存在`
- `400 手机号格式不正确`

### 3.8 `POST /auth/campus/verify`

- 说明：执行校园认证
- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `realName` | string | 是 | 真实姓名 |
| `studentId` | string | 是 | 学号 |

#### 请求示例

```json
{
  "realName": "张三",
  "studentId": "2023123456"
}
```

#### 成功响应

```json
{
  "code": 0,
  "message": "ok",
  "data": null
}
```

#### 说明

- 调用后会更新用户的 `realName`、`studentId`，并将 `campusVerified` 设为 `true`

---

## 4. 用户模块（user-service）

Base URL: `http://127.0.0.1:8082`

### 4.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 健康检查 | `GET` | `/users/ping` | 否 | 服务检查 |
| 获取当前用户 | `GET` | `/users/me` | 是 | 当前登录用户信息 |
| 更新当前用户 | `PUT` | `/users/me` | 是 | 更新昵称、头像 |
| 新增收藏 | `POST` | `/users/me/favorites` | 是 | 收藏商品 |
| 取消收藏 | `DELETE` | `/users/me/favorites/{itemId}` | 是 | 取消商品收藏 |
| 我的收藏列表 | `GET` | `/users/me/favorites` | 是 | 返回收藏商品 |
| 新增浏览记录 | `POST` | `/users/me/browse-history` | 是 | 写入浏览行为 |
| 浏览记录列表 | `GET` | `/users/me/browse-history` | 是 | 返回浏览记录 |
| 创建举报 | `POST` | `/reports` | 是 | 举报商品或用户 |
| 我的举报列表 | `GET` | `/reports/mine` | 是 | 查询本人举报 |
| 管理员举报列表 | `GET` | `/admin/reports` | 是，管理员 | 举报后台列表 |
| 管理员举报详情 | `GET` | `/admin/reports/{reportId}` | 是，管理员 | 举报详情 |
| 通过举报 | `POST` | `/admin/reports/{reportId}/approve` | 是，管理员 | 审核通过，并返回最新举报详情 |
| 驳回举报 | `POST` | `/admin/reports/{reportId}/reject` | 是，管理员 | 审核驳回，并返回最新举报详情 |
| 封禁用户 | `POST` / `PUT` / `PATCH` | `/admin/users/{userId}/ban` | 是，管理员 | 封禁用户 |
| 解封用户 | `POST` / `PUT` / `PATCH` | `/admin/users/{userId}/unban` | 是，管理员 | 解封用户 |
| 管理员下架商品 | `PUT` | `/admin/items/{itemId}/off-shelf` | 是，管理员 | 强制下架 |

### 4.2 `GET /users/ping`

```json
{
  "code": 0,
  "message": "ok",
  "data": "user-service is running"
}
```

### 4.3 `GET /users/me`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `userId` | string | 用户 ID |
| `username` | string | 用户名 |
| `nickname` | string | 昵称 |
| `phone` | string | 手机号 |
| `realName` | string | 真实姓名 |
| `studentId` | string | 学号 |
| `avatarUrl` | string | 头像 URL |
| `campusVerified` | boolean | 是否已校园认证 |
| `creditScore` | integer | 信用分 |
| `creditLevel` | string | 信用等级 |
| `reviewCount` | integer | 评价数 |
| `averageRating` | number | 平均评分 |
| `role` | string | 角色，如 `USER`、`ADMIN` |
| `status` | string | 用户状态，如 `ACTIVE`、`BANNED` |
| `accountStatus` | string | 用户状态别名，值与 `status` 一致 |
| `banned` | boolean | 是否已被封禁 |
| `disabled` | boolean | 是否已被禁用，当前与 `banned` 一致 |

### 4.4 `PUT /users/me`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `nickname` | string | 是 | 新昵称 |
| `avatarUrl` | string | 否 | 头像 URL |

#### 响应

`data` 字段与 `GET /users/me` 一致。

### 4.5 `POST /users/me/favorites`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `itemId` | string | 是 | 商品 ID |

#### 常见错误

- `400 该商品已收藏`
- `404 商品不存在`

### 4.6 `DELETE /users/me/favorites/{itemId}`

- 请求头：`Authorization: Bearer <accessToken>`

#### 路径参数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |

#### 常见错误

- `400 收藏记录不存在`

### 4.7 `GET /users/me/favorites`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |
| `title` | string | 商品标题 |
| `price` | number | 商品价格 |
| `conditionStar` | integer | 成色 1~5 |
| `coverImage` | string | 封面图 |
| `createdAt` | string | 收藏创建时间 |

### 4.8 `POST /users/me/browse-history`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `itemId` | string | 是 | 商品 ID |
| `source` | string | 否 | 浏览来源，约定 `SEARCH`、`RECOMMEND`、`DIRECT`，默认 `DIRECT` |

### 4.9 `GET /users/me/browse-history`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |
| `title` | string | 商品标题 |
| `price` | number | 商品价格 |
| `viewedAt` | string | 浏览时间 |
| `source` | string | 浏览来源 |

### 4.10 `POST /reports`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `reportType` | string | 是 | `ITEM` 或 `USER` |
| `targetId` | string | 是 | 举报目标 ID |
| `reasonCode` | string | 是 | 原因码，按 `reportType` 决定可选集合 |
| `description` | string | 否 | 补充说明 |

#### `reasonCode` 取值

| reportType | 可选值 |
| --- | --- |
| `ITEM` | `ILLEGAL`、`PORN`、`SCAM`、`SPAM`、`OTHER` |
| `USER` | `FRAUD`、`HARASSMENT`、`IMPERSONATION`、`SPAM`、`OTHER` |

#### 常见错误

- `400 reportType仅支持ITEM或USER`
- `400 reasonCode不支持`
- `400 不能举报自己`
- `400 不能举报自己发布的商品`
- `404 商品不存在`
- `404 用户不存在`
- `409 请勿重复提交待处理举报`

### 4.11 `GET /reports/mine`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `reportId` | string | 举报 ID |
| `reportType` | string | 举报类型 |
| `targetId` | string | 举报目标 ID |
| `reporterId` | string | 举报人 ID |
| `targetUserId` | string | 目标用户 ID；`USER` 举报时为被举报用户，`ITEM` 举报时为商品卖家 |
| `itemId` | string | 目标商品 ID；仅 `ITEM` 举报时有值 |
| `reasonCode` | string | 原因码 |
| `description` | string | 举报描述 |
| `status` | string | `PENDING` / `APPROVED` / `REJECTED` |
| `reviewedBy` | string | 审核管理员 ID |
| `reviewedAt` | string | 审核时间 |
| `reviewNote` | string | 审核备注 |
| `createdAt` | string | 创建时间 |
| `updatedAt` | string | 更新时间 |
| `reporter` | object | 举报人展示对象，见下表 |
| `targetUser` | object | 目标用户展示对象；`USER` 举报时为被举报用户，`ITEM` 举报时为商品卖家 |
| `targetItem` | object | 目标商品展示对象；仅 `ITEM` 举报时有值 |

`reporter` / `targetUser` 字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `userId` | string | 用户 ID |
| `username` | string | 用户名 |
| `nickname` | string | 昵称 |

`targetItem` 字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |
| `title` | string | 商品标题 |

说明：

- `targetId` 保留用于兼容旧前端和排障
- 如果目标用户或商品已不存在，对应嵌套对象可能为 `null`

### 4.12 `GET /admin/reports`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员且账号未被封禁

#### Query 参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `status` | string | 否 | 过滤状态，支持 `PENDING`、`APPROVED`、`REJECTED` |

#### 响应

`data[]` 字段与 `GET /reports/mine` 一致。

### 4.13 `GET /admin/reports/{reportId}`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员

#### 路径参数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `reportId` | string | 举报 ID |

#### 响应

`data` 字段与 `ReportItemResponse` 一致，包含：

- 基础举报字段
- `targetUserId`
- `itemId`
- `reporter`
- `targetUser`
- `targetItem`

### 4.14 `POST /admin/reports/{reportId}/approve`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
- 权限：管理员

#### 路径参数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `reportId` | string | 举报 ID |

#### 请求体字段

请求体可为空；非空时支持：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `reviewNote` | string | 否 | 审核备注 |

#### 业务效果

- `ITEM` 举报通过：目标商品被下架
- `USER` 举报通过：目标用户被封禁
- 会向举报人发送内部通知

#### 响应

返回最新的 `ReportItemResponse`，其中 `status` 已更新为 `APPROVED`，前端可直接据此隐藏审核按钮。

### 4.15 `POST /admin/reports/{reportId}/reject`

- 请求规则同审核通过接口
- 业务效果：仅更新举报状态为 `REJECTED`，并通知举报人

#### 响应

返回最新的 `ReportItemResponse`，其中 `status` 已更新为 `REJECTED`，前端可直接据此隐藏审核按钮。

### 4.16 `POST /admin/users/{userId}/ban`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
- 权限：管理员

#### 支持方法

- `POST`
- `PUT`
- `PATCH`

#### 请求体字段

请求体可为空；非空时支持：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `reason` | string | 否 | 封禁原因 |

#### 业务效果

- 将用户状态更新为 `BANNED`

### 4.17 `POST /admin/users/{userId}/unban`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员

#### 支持方法

- `POST`
- `PUT`
- `PATCH`

#### 业务效果

- 将用户状态更新为 `ACTIVE`

### 4.18 `PUT /admin/items/{itemId}/off-shelf`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员

#### 业务效果

- 将商品状态从 `ON_SALE` 更新为 `OFF_SHELF`

---

## 5. 商品与定位模块（item-service）

Base URL: `http://127.0.0.1:8083`

### 5.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 健康检查 | `GET` | `/items/ping` | 否 | 服务检查 |
| 发布商品 | `POST` | `/items` | 是 | 新建商品 |
| 更新商品 | `PUT` | `/items/{itemId}` | 是 | 卖家编辑商品 |
| 商品下架 | `POST` | `/items/{itemId}/off-shelf` | 是 | 卖家主动下架 |
| 商品详情 | `GET` | `/items/{itemId}` | 否 | 获取商品详情 |
| 我的商品 | `GET` | `/items/mine` | 是 | 卖家商品列表 |
| 商品搜索 | `GET` | `/search/items` | 否 | 条件搜索 |
| 商品推荐 | `GET` | `/recommend/items` | 是 | 推荐列表 |
| 新增商品评论 | `POST` | `/items/{itemId}/comments` | 是 | 给商品留言 |
| 商品评论列表 | `GET` | `/items/{itemId}/comments` | 否 | 获取商品评论 |
| 逆地理解析 | `GET` | `/locations/reverse-geocode` | 否 | 经纬度转地点 |
| 地点搜索 | `GET` | `/locations/search` | 否 | 关键词搜索位置 |

### 5.2 `GET /items/ping`

```json
{
  "code": 0,
  "message": "ok",
  "data": "item-service is running"
}
```

### 5.3 `POST /items`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `title` | string | 是 | 商品标题 |
| `description` | string | 是 | 商品描述 |
| `price` | number | 是 | 必须大于 `0` |
| `conditionStar` | integer | 是 | 成色，`1`~`5` |
| `categoryId` | string | 是 | 分类 ID |
| `categoryName` | string | 是 | 分类名称 |
| `tradeMode` | string | 是 | `ONLINE`、`OFFLINE`、`BOTH` |
| `images` | string[] | 否 | 图片 URL 列表，当前实现最多 `9` 张 |
| `expireAt` | string | 否 | 过期时间 |
| `location` | object | 否 | 定位信息，见下表 |

#### `location` 字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `lat` | number | 否 | 纬度；有值时必须与 `lng` 同时提供 |
| `lng` | number | 否 | 经度 |
| `address` | string | 否 | 地址 |
| `poiName` | string | 否 | POI 名称 |
| `coordType` | string | 否 | `WGS84` 或 `GCJ02`，默认 `GCJ02` |
| `source` | string | 否 | 来源，默认写入 `manual` |

#### 常见错误

- `403 当前账号已被封禁，禁止写操作`
- `400 tradeMode仅支持ONLINE、OFFLINE或BOTH`
- `400 location经纬度必须同时提供`
- `400 location坐标超出合法范围`
- `400 商品图片最多9张`

### 5.4 `PUT /items/{itemId}`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
- 路径参数：`itemId`
- 请求体字段：与创建商品一致

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |
| `sellerId` | string | 卖家 ID |
| `title` | string | 商品标题 |
| `description` | string | 商品描述 |
| `categoryId` | string | 分类 ID |
| `categoryName` | string | 分类名称 |
| `price` | number | 价格 |
| `conditionStar` | integer | 成色 |
| `images` | string[] | 图片列表 |
| `coverImage` | string | 封面图 |
| `tradeMode` | string | 交易方式 |
| `location` | object | 定位对象 |
| `distanceMeters` | number | 与请求位置距离，详情接口通常为空 |
| `lat` | number | 顶层纬度镜像 |
| `lng` | number | 顶层经度镜像 |
| `status` | string | `ON_SALE`、`SOLD`、`OFF_SHELF`、`EXPIRED` 等 |
| `seller` | object | 卖家信息 |
| `stats` | object | 浏览/收藏/聊天统计 |
| `createdAt` | string | 创建时间 |

#### `seller` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `userId` | string | 卖家 ID |
| `nickname` | string | 昵称 |
| `avatarUrl` | string | 头像 |
| `creditScore` | integer | 信用分 |
| `creditLevel` | string | 信用等级 |
| `reviewCount` | integer | 评价数 |
| `averageRating` | number | 平均评分 |

#### `stats` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `viewCount` | integer | 浏览数 |
| `favoriteCount` | integer | 收藏数 |
| `chatCount` | integer | 咨询数 |

#### 常见错误

- `404 商品不存在`
- `403 无权限操作该商品`
- `400 已售出商品不可编辑`
- `400 已下架商品不可编辑`
- `400 已过期商品不可编辑`

### 5.5 `POST /items/{itemId}/off-shelf`

- 请求头：`Authorization: Bearer <accessToken>`

#### 常见错误

- `404 商品不存在`
- `400 商品已下架`
- `400 已售出商品不可下架`

### 5.6 `GET /items/{itemId}`

- 说明：公开接口，无需鉴权
- 响应 `data` 字段：与更新商品返回的 `ItemDetailResponse` 一致

### 5.7 `GET /items/mine`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |
| `title` | string | 标题 |
| `price` | number | 价格 |
| `conditionStar` | integer | 成色 |
| `images` | string[] | 图片列表 |
| `coverImage` | string | 封面图 |
| `status` | string | 商品状态 |
| `location` | object | 定位对象 |
| `distanceMeters` | number | 距离，通常为空 |
| `createdAt` | string | 创建时间 |

### 5.8 `GET /search/items`

- 说明：公开搜索接口

#### Query 参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `q` | string | 否 | 关键词，匹配标题、描述、分类名 |
| `categoryId` | string | 否 | 分类 ID |
| `minPrice` | number | 否 | 最低价 |
| `maxPrice` | number | 否 | 最高价 |
| `minCondition` | integer | 否 | 最低成色 |
| `maxCondition` | integer | 否 | 最高成色 |
| `sort` | string | 否 | 当前实现支持 `PRICE`、`HOT`，默认按创建时间倒序 |
| `lat` | number | 否 | 请求位置纬度；有值时必须与 `lng` 同时提供 |
| `lng` | number | 否 | 请求位置经度 |
| `radiusMeters` | integer | 否 | 距离筛选半径，`>0` 时生效 |
| `sortBy` | string | 否 | `distance` 时按距离排序 |
| `page` | integer | 否 | 页码，默认 `1` |
| `pageSize` | integer | 否 | 每页数量，默认 `10` |

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `list` | array | 搜索结果列表 |
| `total` | integer | 总数 |

#### `list[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `itemId` | string | 商品 ID |
| `title` | string | 标题 |
| `price` | number | 价格 |
| `conditionStar` | integer | 成色 |
| `coverImage` | string | 封面图 |
| `hotScore` | integer | 热度分 |
| `location` | object | 定位信息 |
| `distanceMeters` | number | 距离 |
| `seller` | object | 卖家信息 |
| `recommendReason` | string | 推荐原因，推荐接口返回 |
| `sourceChannels` | string[] | 候选来源通道，推荐接口返回 |
| `debugScores` | object | 调试分值，仅 `debug=true` 时返回 |

#### `debugScores` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `profileScore` | number | 画像/内容匹配分 |
| `behaviorSimilarityScore` | number | 相似用户行为分 |
| `associationScore` | number | 关联规则分 |
| `hotnessScore` | number | 热度分 |
| `freshnessScore` | number | 新鲜度分 |
| `distanceScore` | number | 距离分 |
| `totalScore` | number | 综合分 |

### 5.9 `GET /recommend/items`

- 请求头：`Authorization: Bearer <accessToken>`
- Query 参数：`lat`、`lng`、`radiusMeters`、`sortBy`、`page`、`pageSize`、`debug`
- 默认分页：`page=1`、`pageSize=10`
- 响应结构：与搜索接口一致，额外返回 `requestId`

#### 响应 `data` 字段补充

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `requestId` | string | 推荐请求 ID，用于推荐反馈回传 |

### 5.10 `GET /items/{itemId}/similar`

- 请求头：`Authorization: Bearer <accessToken>` 可选
- Query 参数：`lat`、`lng`、`radiusMeters`、`page`、`pageSize`、`debug`
- 默认分页：`page=1`、`pageSize=10`
- 响应结构：与推荐接口一致，返回 `requestId`

### 5.11 `POST /recommend/feedback`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `requestId` | string | 是 | 推荐请求 ID |
| `scene` | string | 是 | `HOME_FEED` / `ITEM_SIMILAR` |
| `eventType` | string | 是 | `CLICK` / `FAVORITE` / `CHAT` / `TRADE` |
| `itemId` | string | 是 | 被反馈的推荐商品 ID |
| `targetItemId` | string | 否 | 目标商品 ID，例如收藏后的商品 |
| `targetConversationId` | string | 否 | 聊天会话 ID |
| `targetTradeId` | string | 否 | 成交记录 ID |

#### 常见错误

- `400 推荐请求不存在或已过期`
- `400 推荐商品不在曝光记录中`
- `403 无权回传该推荐请求`

### 5.12 `POST /items/{itemId}/comments`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `content` | string | 是 | 评论内容 |
| `comment` | string | 否 | `content` 别名 |
| `rating` | integer | 否 | 若提供，必须在 `1`~`5` |

#### 常见错误

- `404 商品不存在`
- `400 评论评分必须在1到5之间`

### 5.13 `GET /items/{itemId}/comments`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `commentId` | string | 评论 ID |
| `userId` | string | 评论用户 ID |
| `nickname` | string | 评论用户昵称 |
| `content` | string | 评论内容 |
| `createdAt` | string | 评论时间 |

### 5.14 `GET /locations/reverse-geocode`

#### Query 参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `lat` | number | 是 | 纬度 |
| `lng` | number | 是 | 经度 |
| `coordType` | string | 否 | `WGS84` 或 `GCJ02`，默认 `WGS84` |

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `lat` | number | GCJ02 纬度 |
| `lng` | number | GCJ02 经度 |
| `address` | string | 格式化地址 |
| `poiName` | string | POI 名称 |
| `coordType` | string | 固定返回 `GCJ02` |
| `source` | string | 固定返回 `device` |

### 5.15 `GET /locations/search`

#### Query 参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `q` | string | 是 | 搜索关键词 |
| `lat` | number | 否 | 参考位置纬度 |
| `lng` | number | 否 | 参考位置经度 |

#### 响应 `data`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `locations` | array | 位置列表 |
| `list` | array | 兼容前端解析的候选列表，内容与 `locations` 一致 |

#### `locations[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `lat` | number | 纬度（GCJ02） |
| `lng` | number | 经度（GCJ02） |
| `address` | string | 地址 |
| `poiName` | string | 地点名 |
| `coordType` | string | 固定 `GCJ02` |
| `source` | string | 固定 `search` |

#### 常见错误

- `400 搜索关键词不能为空`
- `400 搜索定位坐标必须同时提供`
- `500 高德地图Key未配置`
- `502 高德地点搜索调用失败`

---

## 6. 文件模块（file-service）

Base URL: `http://127.0.0.1:8087`

### 6.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 单图上传 | `POST` | `/files/upload/image` | 是 | 上传单张图片 |
| 多图上传 | `POST` | `/files/upload/images` | 是 | 批量上传图片 |
| 文件元数据 | `GET` | `/files/{fileId}` | 否 | 查询文件元数据 |
| 文件预览 | `GET` | `/files/view/{fileId}` | 否 | 直接返回二进制内容 |
| 绑定文件 | `POST` | `/files/bind` | 是 | 绑定业务对象 |
| 删除文件 | `DELETE` | `/files/{fileId}` | 是 | 逻辑删除 |

### 6.2 上传通用约定

| 项 | 说明 |
| --- | --- |
| Content-Type | `multipart/form-data` |
| 支持文件字段名 | `file`、`file[]`、`files`、`files[]` |
| 图片类型 | `image/jpeg`、`image/png`、`image/webp` |
| `ITEM_IMAGE` / `CHAT_IMAGE` | 单次最多 `9` 张，每张最大 `5MB` |
| `AVATAR` | 单次最多 `1` 张，每张最大 `2MB` |

### 6.3 `POST /files/upload/image`

- 请求头：`Authorization: Bearer <accessToken>`
- Content-Type：`multipart/form-data`

#### Form 字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `bizType` | string | 是 | `ITEM_IMAGE`、`AVATAR`、`CHAT_IMAGE` |
| `file` / `files` | file | 是 | 图片文件 |

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `fileId` | string | 文件 ID |
| `bizType` | string | 业务类型 |
| `url` | string | 文件访问 URL |
| `contentType` | string | MIME 类型 |
| `size` | integer | 文件大小，字节 |
| `width` | integer | 图片宽度 |
| `height` | integer | 图片高度 |
| `status` | string | 初始为 `UPLOADED` |

### 6.4 `POST /files/upload/images`

- 请求头：`Authorization: Bearer <accessToken>`
- Content-Type：`multipart/form-data`

#### Form 字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `bizType` | string | 是 | `ITEM_IMAGE`、`AVATAR`、`CHAT_IMAGE` |
| `files` / `file` | file[] | 是 | 图片文件数组 |

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `fileId` | string | 文件 ID |
| `url` | string | 文件 URL |

### 6.5 `GET /files/{fileId}`

- 说明：公开接口

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `fileId` | string | 文件 ID |
| `ownerId` | string | 所有者用户 ID |
| `bizType` | string | 业务类型 |
| `originalName` | string | 原始文件名 |
| `ext` | string | 扩展名 |
| `contentType` | string | MIME 类型 |
| `size` | integer | 大小 |
| `storageType` | string | 固定 `LOCAL` |
| `storagePath` | string | 存储相对路径 |
| `url` | string | 公网访问 URL |
| `width` | integer | 宽 |
| `height` | integer | 高 |
| `status` | string | 文件状态 |
| `bindService` | string | 绑定服务名 |
| `bindBizId` | string | 绑定业务 ID |
| `createdAt` | string | 创建时间 |
| `updatedAt` | string | 更新时间 |

#### 注意

- 当前实现中，即使文件状态为 `DELETED`，该接口仍可能返回元数据

### 6.6 `GET /files/view/{fileId}`

- 说明：直接返回 `ResponseEntity<Resource>`，不是 `ApiResponse`
- 成功返回：
  - `Content-Type`：文件内容类型
  - `Content-Disposition: inline; filename="<fileId>.<ext>"`
- 常见错误：
  - `404 文件不存在`

### 6.7 `POST /files/bind`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `fileIds` | string[] | 是 | 文件 ID 列表 |
| `bindService` | string | 是 | 绑定服务名，当前实现未限制枚举 |
| `bindBizId` | string | 是 | 绑定业务对象 ID |

#### 常见错误

- `404 存在文件不存在`
- `400 已删除文件不可绑定`
- `403 无权限操作该文件`

### 6.8 `DELETE /files/{fileId}`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `fileId` | string | 文件 ID |
| `status` | string | 固定 `DELETED` |

#### 注意

- 当前删除为逻辑删除
- 若文件已删除，再次删除仍返回成功

---

## 7. 交易与自提点模块（trade-service）

Base URL: `http://127.0.0.1:8084`

### 7.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 健康检查 | `GET` | `/trades/ping` | 否 | 服务检查 |
| 创建订单 | `POST` | `/trades` | 是 | 下单 |
| 我的买入订单 | `GET` | `/trades/buying` | 是 | 买家订单列表 |
| 我的卖出订单 | `GET` | `/trades/selling` | 是 | 卖家订单列表 |
| 订单详情 | `GET` | `/trades/{tradeId}` | 否 | 查询订单详情 |
| 取消订单 | `POST` | `/trades/{tradeId}/cancel` | 是 | 买卖双方均可取消待支付订单 |
| 发起支付 | `POST` | `/trades/{tradeId}/pay` | 是 | 当前网关为 Mock |
| 查询支付状态 | `GET` | `/trades/{tradeId}/pay-status` | 是 | 买卖双方可查 |
| 模拟支付 | `POST` | `/trades/{tradeId}/mock-pay` | 是 | 买家模拟支付 |
| 模拟支付成功 | `POST` | `/trades/{tradeId}/mock-pay-success` | 是 | 买家模拟成功支付，生产环境禁用 |
| 发货 | `POST` | `/trades/{tradeId}/deliver` | 是 | 卖家发货 |
| 完成交易 | `POST` | `/trades/{tradeId}/complete` | 是 | 买家确认收货 |
| 确认交易 | `POST` | `/trades/{tradeId}/confirm` | 是 | 当前实现与 complete 相同 |
| 启用自提点列表 | `GET` | `/pickup-points` | 否 | 可用于下单选择 |
| 管理后台自提点列表 | `GET` | `/admin/pickup-points` | 是，管理员 | 包含禁用项 |
| 创建自提点 | `POST` | `/admin/pickup-points` | 是，管理员 | 创建并默认启用 |
| 更新自提点 | `PUT` | `/admin/pickup-points/{pickupPointId}` | 是，管理员 | 更新信息 |
| 启用自提点 | `POST` | `/admin/pickup-points/{pickupPointId}/enable` | 是，管理员 | 改为启用 |
| 禁用自提点 | `POST` | `/admin/pickup-points/{pickupPointId}/disable` | 是，管理员 | 改为禁用 |
| 删除自提点 | `DELETE` | `/admin/pickup-points/{pickupPointId}` | 是，管理员 | 删除 |

### 7.2 交易状态说明

| 维度 | 取值 |
| --- | --- |
| 订单状态 `status` | `WAIT_PAY`、`PAID`、`WAIT_CONFIRM`、`COMPLETED`、`CANCELLED` |
| 支付状态 `payStatus` | `WAIT_PAY`、`PAID`、`CLOSED` |
| 支付渠道 `payChannel` | 当前实现固定走 `MOCK` 网关 |

### 7.3 `GET /trades/ping`

```json
{
  "code": 0,
  "message": "ok",
  "data": "trade-service is running"
}
```

### 7.4 `POST /trades`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `itemId` | string | 是 | 商品 ID |
| `price` | number | 否 | 若商品有价格，服务端优先使用商品当前价格 |
| `sellerId` | string | 否 | 用于二次校验卖家 |
| `deliveryMode` | string | 否 | `DIRECT` 或 `PICKUP_POINT`，默认 `DIRECT` |
| `pickupPointId` | string | 否 | 当 `deliveryMode=PICKUP_POINT` 时必填 |

#### 业务规则

- 下单时商品状态会尝试从 `ON_SALE` 更新为 `RESERVED`
- 不允许买家与卖家是同一人
- 若 `deliveryMode` 缺省，默认 `DIRECT`
- 若商品 `tradeMode=ONLINE`，不允许选择自提点

#### 常见错误

- `400 买家和卖家不能是同一人`
- `400 卖家信息不匹配`
- `400 pickupPointId不能为空`
- `400 deliveryMode仅支持DIRECT或PICKUP_POINT`
- `409 线上交易商品不支持自提点`
- `409 商品已被其他用户占用`
- `409 商品已售出`

### 7.5 `GET /trades/buying`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tradeId` | string | 订单 ID |
| `tradeNo` | string | 订单号 |
| `itemId` | string | 商品 ID |
| `buyerId` | string | 买家 ID |
| `sellerId` | string | 卖家 ID |
| `price` | number | 价格 |
| `deliveryMode` | string | 交付方式 |
| `pickupPointId` | string | 自提点 ID |
| `pickupPointName` | string | 自提点名称 |
| `status` | string | 订单状态 |
| `payStatus` | string | 支付状态 |
| `payChannel` | string | 支付渠道 |
| `outTradeNo` | string | 外部支付单号 |
| `payExpireAt` | string | 支付过期时间 |
| `paidAt` | string | 支付时间 |
| `createdAt` | string | 创建时间 |

### 7.6 `GET /trades/selling`

- 请求头：`Authorization: Bearer <accessToken>`
- 响应结构：与买入订单列表一致

### 7.7 `GET /trades/{tradeId}`

- 说明：当前实现无需鉴权即可访问

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tradeId` | string | 订单 ID |
| `itemId` | string | 商品 ID |
| `buyerId` | string | 买家 ID |
| `sellerId` | string | 卖家 ID |
| `buyerNickname` | string | 买家昵称 |
| `sellerNickname` | string | 卖家昵称 |
| `deliveryMode` | string | 交付方式 |
| `pickupPointId` | string | 自提点 ID |
| `pickupPointName` | string | 自提点名称 |
| `pickupAddress` | string | 自提点地址 |
| `pickupLat` | number | 自提点纬度 |
| `pickupLng` | number | 自提点经度 |
| `pickupContactPhone` | string | 联系电话 |
| `pickupBusinessHours` | string | 营业时间 |
| `price` | number | 订单金额 |
| `status` | string | 订单状态 |
| `tradeNo` | string | 订单号 |
| `payStatus` | string | 支付状态 |
| `payChannel` | string | 支付渠道 |
| `outTradeNo` | string | 外部支付单号 |
| `providerTradeNo` | string | 渠道交易号 |
| `payExpireAt` | string | 支付过期时间 |
| `paidAt` | string | 支付完成时间 |
| `deliveredAt` | string | 发货时间 |
| `confirmedAt` | string | 确认收货时间 |
| `completedAt` | string | 完成时间 |
| `cancelledAt` | string | 取消时间 |
| `cancelReason` | string | 取消原因 |
| `refundReason` | string | 退款原因 |
| `createdAt` | string | 创建时间 |

### 7.8 `POST /trades/{tradeId}/cancel`

- 请求头：`Authorization: Bearer <accessToken>`

#### 业务规则

- 买家、卖家均可取消
- 仅 `WAIT_PAY` 状态允许取消
- 取消成功后会尝试把商品状态从 `RESERVED` 恢复为 `ON_SALE`

#### 常见错误

- `403 无权限操作该交易`
- `409 当前状态不可取消`

### 7.9 `POST /trades/{tradeId}/pay`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
- 请求体可为空

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `channel` | string | 否 | 默认值为 `MOCK`，但当前实现最终仍以激活网关为准 |

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tradeId` | string | 订单 ID |
| `tradeNo` | string | 订单号 |
| `status` | string | 订单状态 |
| `payStatus` | string | 支付状态 |
| `payChannel` | string | 当前渠道，现为 `MOCK` |
| `amount` | number | 金额 |
| `outTradeNo` | string | 外部支付单号 |
| `payExpireAt` | string | 支付过期时间，默认创建后 30 分钟 |
| `paymentData` | object | 支付附加信息 |

#### 当前 `paymentData` 示例

```json
{
  "mode": "MOCK",
  "message": "调用 mock 支付接口完成本地联调",
  "mockAction": "/trades/trade_1001/mock-pay"
}
```

#### 常见错误

- `403 只有买家可以发起支付`
- `409 当前状态不可支付`

### 7.10 `GET /trades/{tradeId}/pay-status`

- 请求头：`Authorization: Bearer <accessToken>`
- 仅买家或卖家可查询

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tradeId` | string | 订单 ID |
| `tradeNo` | string | 订单号 |
| `status` | string | 订单状态 |
| `payStatus` | string | 支付状态 |
| `payChannel` | string | 渠道 |
| `outTradeNo` | string | 外部单号 |
| `providerTradeNo` | string | 渠道单号 |
| `payExpireAt` | string | 过期时间 |
| `paidAt` | string | 支付时间 |
| `updatedAt` | string | 更新时间 |

### 7.11 `POST /trades/{tradeId}/mock-pay`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：买家模拟支付，适合本地联调

#### 常见错误

- `403 只有买家可以模拟支付`
- `409 当前状态不可模拟支付`

### 7.12 `POST /trades/{tradeId}/mock-pay-success`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：将订单直接推进到已支付状态

#### 业务规则

- 仅买家可调用
- 生产环境禁用
- 成功后返回 `TradeDetailResponse`

#### 常见错误

- `403 只有买家可以发起模拟支付`
- `403 生产环境不允许调用模拟支付接口`
- `409 当前订单状态不允许支付`

### 7.13 `POST /trades/{tradeId}/deliver`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：卖家发货

#### 业务规则

- 仅卖家可调用
- 需订单已支付
- 成功后状态推进为 `WAIT_CONFIRM`

#### 常见错误

- `403 只有卖家可以发货`
- `409 订单未支付，不能发货`
- `409 当前状态不可发货`

### 7.14 `POST /trades/{tradeId}/complete`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：买家确认收货并完成交易

#### 业务规则

- 仅买家可调用
- 需订单处于 `WAIT_CONFIRM`
- 调用前会校验商品状态是否已为 `SOLD`

#### 常见错误

- `403 只有买家可以确认收货`
- `409 当前订单未发货，不能确认收货`
- `409 当前状态不可确认收货`

### 7.15 `POST /trades/{tradeId}/confirm`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：当前 Controller 直接调用 `completeTrade`，行为与 `/complete` 一致

### 7.16 `GET /pickup-points`

- 说明：公开接口

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `pickupPointId` | string | 自提点 ID |
| `name` | string | 名称 |
| `campusName` | string | 校区名称 |
| `address` | string | 地址 |
| `lat` | number | 纬度 |
| `lng` | number | 经度 |
| `contactPhone` | string | 联系电话 |
| `businessHours` | string | 营业时间 |
| `status` | string | 状态，公开列表仅返回 `ENABLED` |
| `sortOrder` | integer | 排序值 |
| `createdAt` | string | 创建时间 |
| `updatedAt` | string | 更新时间 |

### 7.17 `GET /admin/pickup-points`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员
- 响应结构：与公开自提点列表一致，但包含全部状态

### 7.18 `POST /admin/pickup-points`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
- 权限：管理员

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `name` | string | 是 | 自提点名称 |
| `campusName` | string | 是 | 校区名称 |
| `address` | string | 是 | 地址 |
| `lat` | number | 是 | 纬度 |
| `lng` | number | 是 | 经度 |
| `contactPhone` | string | 否 | 联系电话 |
| `businessHours` | string | 否 | 营业时间 |
| `sortOrder` | integer | 否 | 默认 `0` |

#### 响应

返回 `PickupPointResponse`，创建时默认状态为 `ENABLED`。

### 7.19 `PUT /admin/pickup-points/{pickupPointId}`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
- 权限：管理员
- 请求体字段：与创建自提点一致

#### 常见错误

- `404 自提点不存在`

### 7.20 `POST /admin/pickup-points/{pickupPointId}/enable`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员
- 业务效果：状态改为 `ENABLED`

### 7.21 `POST /admin/pickup-points/{pickupPointId}/disable`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员
- 业务效果：状态改为 `DISABLED`

### 7.22 `DELETE /admin/pickup-points/{pickupPointId}`

- 请求头：`Authorization: Bearer <accessToken>`
- 权限：管理员

---

## 8. 聊天模块（chat-service）

Base URL: `http://127.0.0.1:8085`

### 8.1 REST 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 创建或获取会话 | `POST` | `/chat/conversations` | 是 | 买家对商品发起会话 |
| 我的会话列表 | `GET` | `/chat/conversations` | 是 | 会话列表 |
| 会话历史消息 | `GET` | `/chat/messages/{conversationId}` | 是 | 历史消息 |
| REST 发送消息 | `POST` | `/chat/messages` | 是 | 调试或非 WebSocket 场景发送 |
| 清空未读数 | `POST` | `/chat/conversations/{conversationId}/read` | 是 | 清空当前用户在该会话下未读 |

### 8.2 `POST /chat/conversations`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `itemId` | string | 是 | 商品 ID |
| `sellerId` | string | 是 | 卖家 ID，用于校验 |

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversationId` | string | 会话 ID |
| `itemId` | string | 商品 ID |
| `buyerId` | string | 买家 ID |
| `sellerId` | string | 卖家 ID |
| `targetUserId` | string | 对方用户 ID |
| `targetNickname` | string | 对方昵称 |
| `targetAvatarUrl` | string | 对方头像 |

#### 常见错误

- `404 商品不存在`
- `400 商品卖家信息缺失`
- `400 卖家信息不匹配`
- `400 不能和自己创建会话`

### 8.3 `GET /chat/conversations`

- 请求头：`Authorization: Bearer <accessToken>`

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `conversationId` | string | 会话 ID |
| `itemId` | string | 商品 ID |
| `itemTitle` | string | 商品标题 |
| `itemPrice` | number | 商品价格 |
| `buyerId` | string | 买家 ID |
| `sellerId` | string | 卖家 ID |
| `targetUserId` | string | 对方用户 ID |
| `targetNickname` | string | 对方昵称 |
| `targetAvatarUrl` | string | 对方头像 |
| `lastMessage` | string | 最后一条消息预览 |
| `unreadCount` | integer | 当前用户未读数 |
| `updatedAt` | string | 最近更新时间 |

### 8.4 `GET /chat/messages/{conversationId}`

- 请求头：`Authorization: Bearer <accessToken>`

#### 路径参数

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `conversationId` | string | 会话 ID |

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `messageId` | string | 消息 ID |
| `conversationId` | string | 会话 ID |
| `senderId` | string | 发送者 ID |
| `type` | string | 消息类型 |
| `content` | string | 消息内容 |
| `seq` | integer | 会话内顺序号，从 `1` 递增 |
| `createdAt` | string | 创建时间 |

#### 常见错误

- `404 会话不存在`
- `403 无权访问该会话`

### 8.5 `POST /chat/messages`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `conversationId` | string | 是 | 会话 ID |
| `type` | string | 是 | 消息类型；当前实现只要求非空，约定值 `TEXT`、`IMAGE` |
| `content` | string | 是 | 消息内容 |

#### 响应 `data`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `messageId` | string | 消息 ID |
| `conversationId` | string | 会话 ID |
| `senderId` | string | 发送者 ID |
| `type` | string | 消息类型 |
| `content` | string | 内容 |
| `seq` | integer | 顺序号 |
| `createdAt` | string | 创建时间 |

#### 常见错误

- `404 会话不存在`
- `403 无权访问该会话`
- `400 消息类型不能为空`
- `400 消息内容不能为空`

### 8.6 `POST /chat/conversations/{conversationId}/read`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：将当前用户在该会话下的未读数置零

#### 成功响应

```json
{
  "code": 0,
  "message": "ok",
  "data": null
}
```

### 8.7 WebSocket 协议

- 连接地址：`ws://127.0.0.1:8085/ws/chat`
- 握手成功后，客户端必须先发送 `AUTH`
- 服务端允许跨域来源来自配置中的前端域名

#### 客户端通用请求格式

```json
{
  "type": "AUTH",
  "requestId": "req_001",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | string | `AUTH`、`TEXT`、`READ` |
| `requestId` | string | 可选但推荐，便于前后端配对 |
| `data` | object | 动态负载 |

#### 服务端通用响应格式

```json
{
  "type": "ACK",
  "requestId": "req_001",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | string | `ACK`、`ERROR`、`TEXT` |
| `requestId` | string | 对应客户端请求 ID；推送消息通常为 `null` |
| `data` | object | 响应体 |

### 8.8 WebSocket: `AUTH`

#### 请求

```json
{
  "type": "AUTH",
  "requestId": "req_auth_1",
  "data": {
    "token": "Bearer xxx"
  }
}
```

#### 成功响应

```json
{
  "type": "ACK",
  "requestId": "req_auth_1",
  "data": {
    "ok": true,
    "userId": "u_1001"
  }
}
```

#### 错误响应

```json
{
  "type": "ERROR",
  "requestId": "req_auth_1",
  "data": {
    "message": "token无效"
  }
}
```

### 8.9 WebSocket: `TEXT`

#### 请求

```json
{
  "type": "TEXT",
  "requestId": "req_text_1",
  "data": {
    "conversationId": "conv_1001",
    "content": "还在吗？"
  }
}
```

#### ACK 响应 / 推送 `TEXT` 负载字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `messageId` | string | 消息 ID |
| `conversationId` | string | 会话 ID |
| `senderId` | string | 发送者 ID |
| `type` | string | 当前固定写入 `TEXT` |
| `content` | string | 消息内容 |
| `seq` | integer | 顺序号 |
| `createdAt` | string | 创建时间 |

#### 说明

- 发送方先收到 `ACK`
- 接收方在线时会收到 `type=TEXT` 的推送消息

### 8.10 WebSocket: `READ`

#### 请求

```json
{
  "type": "READ",
  "requestId": "req_read_1",
  "data": {
    "conversationId": "conv_1001",
    "lastSeq": 12
  }
}
```

#### 成功响应

```json
{
  "type": "ACK",
  "requestId": "req_read_1",
  "data": {
    "ok": true,
    "conversationId": "conv_1001",
    "userId": "u_1001",
    "lastSeq": 12
  }
}
```

#### WebSocket 常见错误

- `消息类型不能为空`
- `不支持的消息类型`
- `未认证，请先发送AUTH消息`
- `消息体不能为空`
- `conversationId不能为空`
- `content不能为空`

---

## 9. 评价模块（review-service）

Base URL: `http://127.0.0.1:8086`

### 9.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 创建评价 | `POST` | `/reviews` | 是 | 针对已完成交易评价对方 |
| 用户评价列表 | `GET` | `/reviews/users/{userId}` | 可匿名，若带 Token 则校验 | 查看某用户收到的评价 |
| 交易评价状态 | `GET` | `/reviews/trades/{tradeId}/status` | 可匿名，若带 Token 则校验 | 查看买卖双方是否已评价 |

### 9.2 `POST /reviews`

- 请求头：
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `tradeId` | string | 是 | 交易 ID |
| `rating` | integer | 是 | `1`~`5` |
| `comment` | string | 否 | 评价内容 |

#### 响应 `data`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `reviewId` | string | 新建评价 ID |

#### 业务规则

- 仅交易买家或卖家可评价
- 仅 `COMPLETED` 订单允许评价
- 同一用户对同一交易只能评价一次
- 创建评价后会重算被评价用户的信用画像

#### 常见错误

- `404 交易不存在`
- `409 当前交易未完成，不能评价`
- `409 您已评价过该交易`
- `403 无权限评价该交易`

### 9.3 `GET /reviews/users/{userId}`

- 请求头：可不传；若传 `Authorization`，服务会校验其格式和有效性

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `reviewId` | string | 评价 ID |
| `tradeId` | string | 交易 ID |
| `fromUserId` | string | 评价发起人 ID |
| `fromUserNickname` | string | 评价发起人昵称 |
| `toUserId` | string | 被评价用户 ID |
| `rating` | integer | 评分 |
| `comment` | string | 评论内容 |
| `createdAt` | string | 创建时间 |

### 9.4 `GET /reviews/trades/{tradeId}/status`

- 请求头：可不传；若传则校验

#### 响应 `data` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `tradeId` | string | 交易 ID |
| `buyerReviewed` | boolean | 买家是否已评价 |
| `sellerReviewed` | boolean | 卖家是否已评价 |

### 9.5 信用等级说明

评价系统当前会根据评分和评价数重算信用信息：

| 等级 | 说明 |
| --- | --- |
| `NEW` | 无评价 |
| `EXCELLENT` | 信用分 `>= 90` |
| `GOOD` | 信用分 `>= 75` |
| `FAIR` | 信用分 `>= 60` |
| `LOW` | 其余情况 |

---

## 10. 通知模块（notice-service）

Base URL: `http://127.0.0.1:8088`

### 10.1 接口概览

| 接口 | 方法 | 路径 | 鉴权 | 说明 |
| --- | --- | --- | --- | --- |
| 通知列表 | `GET` | `/notifications` | 是 | 当前用户通知 |
| 标记单条已读 | `POST` | `/notifications/{notificationId}/read` | 是 | 标记单条 |
| 全部已读 | `POST` | `/notifications/read-all` | 是 | 批量已读 |
| 创建内部通知 | `POST` | `/internal/notifications` | 否（内部接口） | 服务间调用 |

### 10.2 `GET /notifications`

- 请求头：`Authorization: Bearer <accessToken>`

#### Query 参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `status` | string | 否 | `UNREAD` 或 `READ`，不传表示全部 |

#### 响应 `data[]` 字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `notificationId` | string | 通知 ID |
| `recipientUserId` | string | 接收人 ID |
| `category` | string | 通知分类，内部会转大写 |
| `title` | string | 标题 |
| `content` | string | 内容 |
| `isRead` | boolean | 是否已读 |
| `createdAt` | string | 创建时间，`Asia/Shanghai` |
| `actionType` | string | 动作类型 |
| `actionTargetId` | string | 动作目标 ID |
| `extra` | object | 扩展信息 |

#### 常见错误

- `400 status仅支持UNREAD或READ`

### 10.3 `POST /notifications/{notificationId}/read`

- 请求头：`Authorization: Bearer <accessToken>`

#### 常见错误

- `404 通知不存在`

### 10.4 `POST /notifications/read-all`

- 请求头：`Authorization: Bearer <accessToken>`
- 说明：将当前用户所有未读通知批量标记为已读

### 10.5 `POST /internal/notifications`

- 说明：内部接口，当前代码未做额外鉴权校验
- 请求头：`Content-Type: application/json`

#### 请求体字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `recipientUserId` | string | 是 | 接收人用户 ID |
| `category` | string | 是 | 分类，保存时转为大写 |
| `title` | string | 是 | 标题 |
| `content` | string | 是 | 内容 |
| `actionType` | string | 否 | 动作类型 |
| `actionTargetId` | string | 否 | 动作目标 ID |
| `extra` | object | 否 | 扩展字段 |

#### 响应

返回 `NotificationResponse`，字段与通知列表单项一致。

---

## 11. 附录

### 11.1 用户与管理员约束

- 多个服务在写操作前都会校验用户是否存在、是否被封禁
- 管理接口要求：
  - 用户存在
  - 用户状态不是 `BANNED`
  - 用户角色为 `ADMIN`

### 11.2 文件与商品的典型调用链

1. 先调用文件上传接口，获取 `fileId` 和 `url`
2. 业务侧使用返回的 `url` 写入商品、头像、聊天消息等业务对象
3. 如需维护文件归属，可再调用 `/files/bind`

### 11.3 当前实现中的注意事项

- `GET /trades/{tradeId}` 当前无需鉴权；若后续补安全控制，前端调用策略需要同步调整。
- `GET /files/{fileId}` 当前可能返回状态为 `DELETED` 的文件元数据，但 `GET /files/view/{fileId}` 会对已删除文件返回 `404`。
- `POST /trades/{tradeId}/confirm` 当前实现与 `POST /trades/{tradeId}/complete` 完全一致。
- 聊天 REST 发消息接口对 `type` 未严格限制枚举，只要求非空；前端仍建议按 `TEXT`、`IMAGE` 使用。
- 通知内部接口 `/internal/notifications` 当前未做额外签名或内部鉴权，部署时应通过网关、内网或服务网段约束访问。
